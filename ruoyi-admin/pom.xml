<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>ruoyi</artifactId>
        <groupId>com.ruoyi</groupId>
        <version>3.9.2</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <artifactId>ruoyi-admin</artifactId>

    <description>
        web服务入口
    </description>

    <dependencies>

        <!-- spring-boot-devtools -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional> <!-- 表示依赖不会传递 -->
        </dependency>

        <!-- spring-doc -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

         <!-- Mysql驱动包 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>

        <!-- 核心模块-->
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-framework</artifactId>
        </dependency>

        <!-- 定时任务-->
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-quartz</artifactId>
        </dependency>

        <!-- 代码生成-->
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-generator</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <addResources>true</addResources>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                小程序模板同步：后台「一键打包小程序代码包」(/biz/codepack/{merchantId})
                读的是 classpath:template/miniprogram7/。

                这份模板原先是手工从 miniprogram7/ 复制的一份副本，结果它停在 2026-08-06，
                此后 miniprogram7/ 又改了 59 个 commit（下单页选券入口、门店评分、客服电话
                门店优先兜底、预约类型字典化、requiredPrivateInfos 等）一次都没同步过去。
                商家从后台下载到的始终是三周前的旧代码 —— 表现就是「代码里明明改好了，
                真机上还是没有」，例如到店自取下单页看不到优惠券入口。

                改为构建期从唯一源 miniprogram7/ 自动拷贝，副本不再入库，从根上消除漂移。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-miniprogram-template</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}/template/miniprogram7</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}/../miniprogram7</directory>
                                    <!-- 开发产物 / 工具私有配置 / 本地调试脚本不进商家代码包 -->
                                    <excludes>
                                        <exclude>node_modules/**</exclude>
                                        <exclude>tests/**</exclude>
                                        <exclude>package.json</exclude>
                                        <exclude>package-lock.json</exclude>
                                        <exclude>project.private.config.json</exclude>
                                        <exclude>vitest.config.*</exclude>
                                        <exclude>**/.bak/**</exclude>
                                        <exclude>**/*.bak</exclude>
                                        <exclude>DEBUG*.md</exclude>
                                        <exclude>DEBUG*.sql</exclude>
                                        <exclude>README.md</exclude>
                                    </excludes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>   
                <groupId>org.apache.maven.plugins</groupId>   
                <artifactId>maven-war-plugin</artifactId>   
                <version>3.1.0</version>   
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <warName>${project.artifactId}</warName>
                </configuration>   
           </plugin>   
        </plugins>
        <finalName>${project.artifactId}</finalName>
    </build>

</project>