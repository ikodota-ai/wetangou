<view class="page">
  <view class="head card">
    <text class="title">今日买单流水</text>
    <text class="sub">共 {{list.length}} 条</text>
  </view>
  <view class="empty" wx:if="{{!list.length && loaded}}">
    <text>今日还没有买单记录</text>
  </view>
  <view class="list" wx:else>
    <view class="item card" wx:for="{{list}}" wx:key="billId">
      <view class="row1">
        <text class="no">{{item.billNo}}</text>
        <text class="amount">¥{{item.payAmount}}</text>
      </view>
      <view class="row2">
        <text class="meta">金额 ¥{{item.amount}}  优惠 ¥{{item.discountAmount}}</text>
        <text class="status s-{{item.status}}">{{item.statusText}}</text>
      </view>
      <!-- 这里原有一个「确认买单」按钮（bindtap="onConfirm"），但 index.js 里
           从来没有 onConfirm 这个方法，点了没反应；而它调用的 confirm 端点
           要求 userType=='store'，商家端签发的是 merchant/owner/manager/staff
           也进不去。现在门店默认自动确认，买单建单即待支付，本就不需要这个动作。 -->
      <view class="row3">
        <text class="time">{{item.createTimeStr}}</text>
      </view>
    </view>
  </view>
</view>
