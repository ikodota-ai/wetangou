<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.biz.mapper.StoredCardMapper">

    <resultMap id="BaseResult" type="com.ruoyi.biz.domain.StoredCard">
        <id     property="cardId"         column="card_id"/>
        <result property="merchantId"     column="merchant_id"/>
        <result property="memberId"       column="member_id"/>
        <result property="productId"      column="product_id"/>
        <result property="orderId"        column="order_id"/>
        <result property="faceValue"      column="face_value"/>
        <result property="balance"        column="balance"/>
        <result property="usedAmount"     column="used_amount"/>
        <result property="rechargeAmount" column="recharge_amount"/>
        <result property="refundAmount"   column="refund_amount"/>
        <result property="expireAt"       column="expire_at"/>
        <result property="status"         column="status"/>
        <result property="delFlag"        column="del_flag"/>
        <result property="createBy"       column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"       column="update_by"/>
        <result property="updateTime"     column="update_time"/>
        <result property="remark"         column="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        card_id, merchant_id, member_id, product_id, order_id, face_value, balance,
        used_amount, recharge_amount, refund_amount, expire_at, status, del_flag,
        create_by, create_time, update_by, update_time, remark
    </sql>

    <select id="selectById" resultMap="BaseResult">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_member_stored_card
        WHERE card_id = #{cardId} AND del_flag = '0'
    </select>

    <select id="selectForUpdate" resultMap="BaseResult">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_member_stored_card
        WHERE card_id = #{cardId} AND del_flag = '0'
        FOR UPDATE
    </select>

    <select id="selectByMemberAndProduct" resultMap="BaseResult">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_member_stored_card
        WHERE member_id = #{memberId}
          AND product_id = #{productId}
          AND del_flag = '0'
        LIMIT 1
    </select>

    <select id="selectList" resultMap="BaseResult" parameterType="com.ruoyi.biz.domain.StoredCard">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_member_stored_card
        <where>
            del_flag = '0'
            <if test="merchantId != null"> AND merchant_id = #{merchantId}</if>
            <if test="memberId != null">   AND member_id = #{memberId}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.ruoyi.biz.domain.StoredCard" useGeneratedKeys="true" keyProperty="cardId">
        INSERT INTO biz_member_stored_card (
            merchant_id, member_id, product_id, order_id, face_value, balance,
            used_amount, recharge_amount, refund_amount, expire_at, status, del_flag,
            create_by, create_time, update_by, update_time, remark
        ) VALUES (
            #{merchantId}, #{memberId}, #{productId}, #{orderId}, #{faceValue}, #{balance},
            #{usedAmount}, #{rechargeAmount}, #{refundAmount}, #{expireAt}, #{status}, #{delFlag},
            #{createBy}, NOW(), #{updateBy}, NOW(), #{remark}
        )
    </insert>

    <update id="update" parameterType="com.ruoyi.biz.domain.StoredCard">
        UPDATE biz_member_stored_card
        <set>
            <if test="orderId != null"> order_id = #{orderId},</if>
            <if test="faceValue != null"> face_value = #{faceValue},</if>
            <if test="balance != null"> balance = #{balance},</if>
            <if test="usedAmount != null"> used_amount = #{usedAmount},</if>
            <if test="rechargeAmount != null"> recharge_amount = #{rechargeAmount},</if>
            <if test="refundAmount != null"> refund_amount = #{refundAmount},</if>
            <if test="expireAt != null"> expire_at = #{expireAt},</if>
            <if test="status != null"> status = #{status},</if>
            <if test="delFlag != null"> del_flag = #{delFlag},</if>
            <if test="updateBy != null"> update_by = #{updateBy},</if>
            update_time = NOW(),
        </set>
        WHERE card_id = #{cardId}
    </update>

    <update id="updateBalance" parameterType="com.ruoyi.biz.domain.StoredCard">
        UPDATE biz_member_stored_card
        SET balance = #{balance},
            used_amount = #{usedAmount},
            recharge_amount = #{rechargeAmount},
            refund_amount = #{refundAmount},
            update_by = #{updateBy},
            update_time = NOW()
        WHERE card_id = #{cardId} AND del_flag = '0'
    </update>
</mapper>
