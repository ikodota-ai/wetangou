<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.biz.mapper.MerchantUserMapper">

    <resultMap type="MerchantUser" id="MerchantUserResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="userType"    column="user_type"    />
        <result property="agentId"    column="agent_id"    />
        <result property="merchantId"    column="merchant_id"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <select id="selectMerchantUserByUserId" parameterType="Long" resultMap="MerchantUserResult">
        select id, user_id, user_type, agent_id, merchant_id, create_by, create_time
        from biz_merchant_user where user_id = #{userId}
    </select>

    <insert id="insertMerchantUser" parameterType="MerchantUser" useGeneratedKeys="true" keyProperty="id">
        insert into biz_merchant_user (user_id, user_type, agent_id, merchant_id, create_by, create_time)
        values (#{userId}, #{userType}, #{agentId}, #{merchantId}, #{createBy}, sysdate())
    </insert>

    <update id="updateMerchantUser" parameterType="MerchantUser">
        update biz_merchant_user
        <trim prefix="SET" suffixOverrides=",">
            <if test="userType != null">user_type = #{userType},</if>
            <if test="agentId != null">agent_id = #{agentId},</if>
            <if test="merchantId != null">merchant_id = #{merchantId},</if>
        </trim>
        where user_id = #{userId}
    </update>

    <delete id="deleteMerchantUserByUserId" parameterType="Long">
        delete from biz_merchant_user where user_id = #{userId}
    </delete>

</mapper>
