######################################################################
# Build Tools

.gradle
/build/
!gradle/wrapper/gradle-wrapper.jar

target/
!.mvn/wrapper/maven-wrapper.jar

######################################################################
# IDE

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### JRebel ###
rebel.xml

### NetBeans ###
nbproject/private/
build/*
nbbuild/
dist/
nbdist/
.nb-gradle/

######################################################################
# Others
*.log
*.xml.versionsBackup
*.swp

!*/build/*.java
!*/build/*.html
!*/build/*.xml

# 业务约定：以下目录/文件不入库
# 1. macOS 资源分支
.DS_Store
*/.DS_Store

# 2. 微信开发者工具备份/旧版本
miniprogram6.zip
miniprogram6/
miniprogram7.zip

# 3. 设计稿与中间产物（PRD 已纳入 doc/，原始 zip 不入库）
doc/html-pixso设计文件.zip
doc/html-完整设计文件.zip
doc/prod_img.zip
doc/prod_img/
doc/html/
doc/preview/
doc/.DS_Store

# 4. logs 目录
logs/
*.log

# 5. build 产物
/bin/

# 6. v2 升级参考素材（仅根目录原图不入库；分类整理后的子目录 + INDEX.md 入库）
/doc/抖音来客/微信图片_*.jpg
# 兜底：匹配具体文件名前缀（防止中文路径匹配失败）
# 抖音来客 / 抖音顾客端的手机截图：23MB 二进制，只用于对齐产品形态，
# 结论已经写进 doc/商品字段缺口与抖音来客对齐方案-2026-08-27.md，图本身不入库。
/doc/抖音来客/**/*.jpg
/doc/抖音来客/**/*.jpeg
/doc/抖音来客/**/*.png
/doc/抖音/

# 7. 工具/依赖产物（不入库）
miniprogram7/node_modules/
miniprogram7/package-lock.json
# 注：miniprogram7/tests/ 曾整目录忽略，但它装的是 5 个真 vitest 单测
# （npm test 67 项，CI 也跑），把测试排除在版本控制外会让「反向验证过的护栏」
# 只存在于本地。inviteScene / role 两个是当初 git add -f 进来的，
# 于是同一目录半数入库半数不入库。改为只忽略依赖产物。
.agents/
.playwright-cli/
skills-lock.json

# 8. 运维脚本（不入仓）
doc/*.sh
sql/*.bak
scripts/e2e/*.class
miniprogram7/pages/login/.bak/

# 前端部署包（本地产物，勿入库）
ruoyi-ui/dist.zip

# 小程序代码包模板不再入库：它曾是 miniprogram7/ 的手工副本，停在 2026-08-06,
# 之后漂了 59 个 commit 没人同步，商家从后台下载到的一直是旧代码。
# 现由 ruoyi-admin/pom.xml 在 generate-resources 阶段自动拷贝，唯一源是 miniprogram7/。
ruoyi-admin/src/main/resources/template/miniprogram7/
