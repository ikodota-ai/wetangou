# 2026-08-15 续篇 C19~C22 总览

## 进度
- 基线: 71/71 (C18 后 31 smoke + 10 JUnit + 30 vitest)
- 现在: **74/74** (34 smoke + 10 JUnit + 30 vitest, +3 smoke 0 退化)
- 新增 commit: 3 个,新增 doc: 4 个,真实 P1 修复: 2 处 (1 个文件)

## 提交历史
| Commit | 内容 |
|---|---|
| 14e14a02 | test(smoke): 员工工作台完整链路 C20 21/21 PASS |
| 1c25c61f | test(smoke): 推客端中心+加入+提现+粉丝 C21 12/12 PASS |
| 6fe270db | fix(api)+test(smoke): /api/booking/signup/{id} phone/storePhone 脱敏修复 + C22 13/13 PASS |

## C19 决策 (账单链路)
**跳过** — 已被 c4 9/9 完全覆盖 (create+detail+prepay+pay+confirm+no auth+DB 状态机验证)。
新增 smoke 价值 0,不重复造轮子。

## C20 员工工作台 (21/21)
**首次独立覆盖 6 端点**:
- GET /me, GET /home (5 字段聚合), GET /today/{orders,bills,bookings}, GET /booking/signup/list
- POST /switch-store (范围内 200→101 + 持久化)
- POST /logout (no-op)
- 防御: switch-store 范围外拒绝 + member→/me userType 拒绝 (code 403 "此操作仅限门店端员工")

## C21 推客端 (12/12)
**首次独立覆盖 6 端点**:
- GET /center (非推客 data=null + 推客档案聚合)
- POST /join (防重: 二次 join 返同 distributorId,DB COUNT=1)
- GET /commission/list, GET /withdraw/list
- POST /withdraw (超额 "余额不足" 防御)
- GET /fans
- 未覆盖: agent/summary (需代理商账号), qrcode (c6 已覆盖), withdraw 成功路径 (需 fixture 余额)

## C22 报名详情 (13/13) + 真实 P1 修复 × 2
**新增端点**: GET /api/booking/signup/{id} 端到端
**摸出真实业务缺陷**:
- `vo.put("phone", signup.getPhone())` 明文 → 改 `DesensitizedType.PHONE.desensitizer().apply()`
- `vo.put("storePhone", signup.getStorePhone())` 同样明文 → 同样改
**根因**: 手工 LinkedHashMap 绕开 Jackson @Sensitive 序列化器
**效果**: phone=138****8002 / storePhone=134****3069 (中间 4 位 \*)

## 真实业务缺陷累计 (本轮)
- C20: 0 个新缺陷
- C21: 0 个新缺陷
- C22: **2 个 P1 脱敏缺陷** (已修)

加上 handoff 历史的 12 个,本项目累计 14 个真实业务缺陷已摸出+修复。

## 剩余可推工作
按价值降序:
1. **`/api/distributor/agent/summary`** — 代理商佣金概览,需建代理商 fixture
2. **`/api/distributor/withdraw` 成功路径** — 需 fixture availableAmount>0
3. **`/api/agreement` 列表全 type** — c14 走过 distributor,user/privacy 待补
4. **`/api/pay/notify`** — 支付回调,通常 mock 难测
5. **`/api/booking/{bookingId}`** — c17 走过, 增量价值低
6. **v2 audit P1**: 字典化 / 菜单权限 / 商品创建入口 — 真实业务功能,需 commit 大量代码
7. **v2 audit P2**: 登录入口 userType 路由分流 (8-02 计划, 0.6 天工作量)

## 全套回归 74/74
- 34 smoke (c1~c18 + c20 + c21 + c22 + subitem + e4/e10/e11/e13~e19/e21 + g6)
- 10 JUnit (ruoyi-system 全部)
- 30 vitest (miniprogram7 全部)
