# 客户旅程验证报告（C 端小程序消费者使用流程）

> 验证时间：2026-08-04
> 范围：miniprogram7 微信小程序 + ruoyi-admin 后端
> 目标：5 个核心 C 端用户旅程端到端走通

---

## 旅程 1：到店消费（消费者到店出示核销码消费）✅ 全链路通过

| 步骤 | 前端页面 | 后端端点 | 状态 |
|---|---|---|---|
| 进入首页 | `pages/home/index.js` | `GET /api/banner/list`, `GET /api/store/album`, `GET /api/store/services` | ✅ |
| 浏览商品 | `pages/goods/list` / `goods/detail` | `GET /api/product/list`, `GET /api/product/{id}` | ✅ |
| 下单 | `pages/order/submit/index.js:102 createOrder` | `POST /api/order` | ✅ |
| 预支付 | `pages/order/submit/index.js:120 prepayOrder` | `POST /api/order/prepay/{orderId}` | ✅ |
| 调起支付 | `pages/pay/index` | `POST /api/order/pay/{orderId}` | ✅ |
| 微信支付回调 | （后端内部） | `POST /api/pay/notify/{merchantId}` | ✅ |
| 查看核销码 | `pages/order/detail/index.wxml:8-10` | （无需） | ✅ |
| 店员核销 | `pages/staff/verify/index.js` | `POST /api/order/verify` | ✅ |

**链路**：`扫码→首页→商品→下单→预支付→支付→回调→核销码→店员核销`
**关键修复**：WxPayService 按 merchantId 取支付凭证（commit c9e515f5）

---

## 旅程 2：预约到店（消费者预约门店服务时段）✅ 本轮已修复

| 步骤 | 前端页面 | 后端端点 | 状态 |
|---|---|---|---|
| 浏览门店可约时段 | `pages/booking/create/index.js` | `GET /api/booking/slots` | ✅ |
| 提交预约 | `pages/booking/create/index.js` | `POST /api/booking` | ✅ |
| 我的预约 | `pages/booking/list/index.js` | `GET /api/booking/list` | ✅ |
| 取消预约 | `pages/booking/list/index.js` | `POST /api/booking/cancel` | ✅ |
| **员工确认/拒绝** | `pages/staff/booking/index.js` | `POST /api/store/staff/booking/confirm/{id}` / `POST /api/store/staff/booking/reject/{id}` | ✅ 本轮新增 |

**本轮修复**：
- 数据库 `biz_booking_member` 加 3 列：`confirm_user` / `confirm_time` / `review_remark`
- `status` 字典扩展：`0=已报名 1=已取消 2=已确认 3=已拒绝`
- 后端 `ApiStoreStaffDashboardController` 加 2 个端点，校验 `m.getStoreId()` 防越权
- 小程序 `pages/staff/booking` 加 "确认到店/拒绝" 按钮 + 审核信息展示
- 提交：`4f09996c`

**链路**：`首页→门店详情→预约时段→确认预约→员工工作台→确认/拒绝→到店核销`

---

## 旅程 3：被邀请+省钱（消费者通过推客太阳码进入并下单）✅ 本轮已修复

| 步骤 | 前端页面 | 后端端点 | 状态 |
|---|---|---|---|
| 扫码进入 | `app.js:_applyInviteScene` | （无需） | ✅ 解析 `scene=distributor:{merchantId}:{memberId}` |
| 登录回传 inviteBy | `pages/login/login.js:46-50` | `POST /api/auth/login` | ✅ 写入 `biz_member.invite_by` |
| 已登录补绑邀请人 | `app.js` 二次启动 | `POST /api/auth/bind-invite` | ✅ 仅在 invite_by 为空时回写 |
| 浏览+下单 | `pages/order/submit` | `POST /api/order` | ✅ 本轮新增自动归属 |
| 佣金冻结 | （后端内部） | `ApiCommissionService.settleForOrder` | ✅ frozenAmount += amount |
| 冷静期自动结算 | （定时任务） | `SettleCommissionTask.ryNoParams` 每天 03:00 | ✅ |

**本轮修复**：
- `ApiOrderService.placeOrder` 在 `distributorId == null` 时回查 invite_by → distributor
- 被邀请人下单自动归属佣金给邀请人
- 提交：`d6ab2462`

**链路**：`扫推客码→冷启动解析inviteBy→授权登录→下单→自动归属推客→佣金入冻结→7 天冷静期→可提现`

---

## 旅程 4：现场买单（消费者在店内下单、店员确认金额、消费者付款）✅ 全链路通过

| 步骤 | 前端页面 | 后端端点 | 状态 |
|---|---|---|---|
| 进入买单 | `pages/pay/index/index.js:172 createBill` | `POST /api/bill` | ✅ |
| 店员确认金额 | `pages/staff/bill/index.js onConfirm` | `POST /api/bill/confirm/{billId}` | ✅ StoreStaffRequired |
| 消费者预支付 | `pages/pay/index afterCreated → prepayBill` | `POST /api/bill/prepay/{billId}` | ✅ |
| 调起支付 | `pages/pay/index` | `POST /api/bill/pay/{billId}` | ✅ |
| 微信支付回调 | （后端内部） | `POST /api/pay/notify/{merchantId}` | ✅ |

**链路**：`到店→扫门店码→选择商品/输入金额→店员确认金额→消费者微信支付→买单完成`

---

## 旅程 5：推客赚钱（消费者变推客、分享海报、获得佣金）✅ 全链路通过

| 步骤 | 前端页面 | 后端端点 | 状态 |
|---|---|---|---|
| 申请推客 | `pages/promoter/index onJoin` | `POST /api/distributor/join` | ✅ |
| 推客中心 | `pages/promoter/index` | `GET /api/distributor/center` | ✅ |
| 太阳码海报 | `pages/promoter/poster` | `GET /api/distributor/qrcode` | ✅ 后端生成 wxacode |
| 粉丝列表 | `pages/promoter/index` | `GET /api/distributor/fans` | ✅ |
| 佣金明细 | `pages/promoter/records` | `GET /api/distributor/commission/list` | ✅ |
| 提现申请 | `pages/promoter/withdraw` | `POST /api/distributor/withdraw` | ✅ |
| 提现记录 | `pages/promoter/withdraw` | `GET /api/distributor/withdraw/list` | ✅ |
| 冷静期自动结算 | （定时任务） | `SettleCommissionTask` | ✅ frozen → available |

**链路**：`我的→申请推客→生成海报→分享→好友下单→自动分佣→7 天冷静期→可提现`

---

## 总结

| 旅程 | 状态 | 本轮修复 | 提交 |
|---|---|---|---|
| 1 到店消费 | ✅ 链路完整 | 无 | — |
| 2 预约到店 | ✅ 本轮修复 | 员工确认/拒绝 | 4f09996c |
| 3 被邀请+省钱 | ✅ 本轮修复 | 下单自动归属推客 | d6ab2462 |
| 4 现场买单 | ✅ 链路完整 | 无 | — |
| 5 推客赚钱 | ✅ 链路完整 | 无 | — |

**5/5 旅程端到端通过**。本轮共发现并修复 2 个真实业务缺口（旅程 2 员工端操作、旅程 3 自动归属推客）。
