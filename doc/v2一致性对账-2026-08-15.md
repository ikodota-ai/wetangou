# v2 抖音来客商品模型一致性对账 · 2026-08-15

> **范围**：8-14 audit (`doc/v2升级一致性审计-2026-08-14.md`) 标 P1/P2 缺口的闭环验证
> **方法**：DB 实地查询 + 小程序端到端调用 + admin 编译 + smoke 全量回归
> **结论**：v2 落地 **100%**（P1 全部实装，P2 文档对账一致）

## §0 TL;DR

| 维度 | 状态 | 证据 |
|---|---|---|
| 商家端商品创建入口 | ✅ **已实装且端到端 PASS** | `miniprogram7/pages/merchant/product/create/` 完整 363 行 + staff001 实测创建 productId=999303 |
| 商家端首页触发 | ✅ 实装 | `home/index.wxml:59 bindtap="goCreateProduct"` → `home/index.js:79 navigateTo` |
| 后端 API 链路 | ✅ 全在 | `ApiProductController.add` (108 行 `@PostMapping("/add")`) + `BizProductTypeController.appCreatable` |
| 菜单/权限 | ✅ 入库 | `sys_menu` 中 `biz:productType:*` 6 + `biz:productSubitem:*` 5 + `biz:staffInvite:*` 5 |
| admin 端字典化 | ✅ 已字典化 | `product/index.vue:160 typeList v-for` + `typeText(typeCode)` 走字典（仅 7 个 `v-if="isType(...)"` 是字段显隐，非类型选项） |
| SQL 11 种 typeCode | ✅ DB 11 行 | `biz_product_type` 实测 11 种（GROUPON/VOUCHER/TIMECARD/STORED_CARD/PERIOD_CARD/HUIXIANG_CARD/PRESALE/PICKUP_VOUCHER/COMBO/BILL/BOOKING） |
| 三方对账 | ✅ 一致 | PRD §2.2 (11 种) ↔ SQL (11 种) ↔ admin (字典化，无 hardcode) ↔ mini (字典化 + APP_CREATABLE 过滤) |
| E11~E19 越权 guard | ✅ **全闭环** | 12/12 smoke PASS（c1/subitem/e4/e10/e11/e13~e19）+ 10/10 JUnit PASS |

## §1 P1 商家端商品创建 · 端到端验证

### 1.1 链路
- 入口：员工工作台首页"创建商品"按钮（`home/index.wxml:59` `bindtap="goCreateProduct"`）
- 跳转：`home/index.js:79 goCreateProduct()` → `wx.navigateTo({url: '/pages/merchant/product/create/index'})`
- 页面：`pages/merchant/product/create/index.{js,wxml,wxss,json}` 363 行完整
  - 拉类型：`loadTypes()` 调 `api.productTypeAppCreatable()` → `GET /biz/productType/appCreatable`
  - 拉分类：`loadMe()` 调 `api.categoryList({storeId})` → `GET /api/product/category/list`
  - 提交：`onSubmit()` 调 `api.productAdd(payload)` → `POST /api/product/add`

### 1.2 端到端实测
```bash
# 1) 员工登录
TOKEN=$(curl -X POST -H "Content-Type: application/json" \
  -d '{"username":"staff001","password":"admin123"}' \
  http://localhost:8080/api/store/staff/login | jq -r .token)

# 2) 创建商品
curl -X POST -H "Content-Type: application/json" -H "Authorization: Bearer $TOKEN" \
  -d '{"typeCode":"GROUPON","productName":"smoke-商家端创建入口","price":9.9,
       "storeId":200,"storeIds":"200","validityDays":30,"bookingRequired":0,
       "stock":0,"limitPerUser":0,"maxPerOrder":1,"status":"0"}' \
  http://localhost:8080/api/product/add
# → {"msg":"操作成功","code":200,"productId":999303,"merchantId":1,"storeId":200}

# 3) 列表验证
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/product/list?pageNum=1&pageSize=5&storeId=200"
# → 包含 productId=999303, createBy=merchant_7
```

**端到端 PASS**。

## §2 三方 typeCode 对账

| 序号 | typeCode | 名称 | PRD | SQL | admin 下拉 | mini create |
|---|---|---|---|---|---|---|
| 1 | GROUPON | 团购套餐 | ✅ | ✅ | ✅ 字典 | ✅ APP_CREATABLE |
| 2 | VOUCHER | 代金券 | ✅ | ✅ | ✅ 字典 | ✅ APP_CREATABLE |
| 3 | TIMECARD | 次卡 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 4 | STORED_CARD | 储值卡 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 5 | PERIOD_CARD | 周期卡 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 6 | HUIXIANG_CARD | 惠享卡 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 7 | PRESALE | 预售券 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 8 | PICKUP_VOUCHER | 提货券 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 9 | COMBO | 组合券包 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 10 | BILL | 到店买单 | ✅ | ✅ | ✅ 字典 | (admin-only) |
| 11 | BOOKING | 预约服务 | ✅ | ✅ | ✅ 字典 | (admin-only) |

**三方对账 = 11/11 一致**。

## §3 E11~E19 越权防护闭环

### 3.1 历史问题
8-14 audit 标 "21 controller 越权 guard 全部落地" 但 8-15 早上回归 E13/E14/E15/E16/E17/E19 fail — 真因是 **fixture (sql/smoke-e13-e17-fixture.sql) 从未在 Navicat 执行过**（DB 中 biz_pay_bill/voucher/withdraw/settle*/commission 999x 行均不存在），controller 走 `if (entity != null) assertDataScope(...)` 路径因为 entity==null 直接返 200。

### 3.2 修复步骤
1. `mysql < sql/smoke-e13-e17-fixture.sql` 装入 19 张表测试数据（idempotent ON DUPLICATE KEY UPDATE）
2. E19 缺 commission 999201/999202 + voucher 999501/999502 fixture — 补 SQL
3. E19 commission 999x 在 c1 跑时会被 `smoke-c1.sh` 删除防污染总额断言 — 改 c1 末尾 idempotent 重插（不影响 c1 自身断言，只保证 c1 跑后 E15 仍可用）

### 3.3 回归结果
- **12/12 smoke PASS**：
  - c1 (PASSED) / subitem (PASSED) / e4 (PASSED) / e10 (PASSED) / e11 (PASSED)
  - e13 (5/5) / e14 (15/15) / e15 (18/18) / e16 (18/18) / e17 (9/9) / e18 (15/15) / e19 (8/8)
- **10/10 JUnit PASS**：`mvn test -pl ruoyi-system` (CommissionMapperTest 3 + AgentServiceImplTest 7)
- 后端编译：`mvn -o clean install -DskipTests -pl ruoyi-admin -am` BUILD SUCCESS 45s

## §4 P2 登录分流 · 待用户决策

`doc/AGENTS.md` 8-02 计划 userType 路由分流仍未做：
- `LoginUser/getInfo` 已有 `userType/agentId/merchantId` ✅
- `login.vue` 顶部 userType 切换 tabs 未加 ❌
- `user.js` 登录成功按 userType 跳转未做 ❌
- 路由 + 菜单按 userType 过滤未做 ❌
- 代理商/商户工作台（agent/index, merchant/index）未建 ❌

工作量约 0.6-1 天，需用户决定是否本轮做。

## §5 总结
- **v2 PRD 全部 P0/P1 已闭环**（含端到端验证）
- **P2 文档对账三方一致**（11 种 typeCode）
- **E11~E19 越权防护真闭环**（12 smoke + 10 JUnit 全 PASS）
- **遗留 P2 登录分流**（待用户决策）
