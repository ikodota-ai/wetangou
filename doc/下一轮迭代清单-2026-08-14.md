# 下一轮迭代清单（2026-08-14 · 56 commit 落地后）

> 来源：8-14 session 主动审计发现的「产品增量机会」+ 0 行表（缺种子）+ 缺单元测试覆盖。
> 工作量单位：d（人/天）。

## 优先级矩阵

| 优先级 | 类别 | 项 | 工作量 | 状态 | 证据 |
|--------|------|-----|--------|------|------|
| 🟡 P1 | 安全 | E1 ApiProductController subitemGroups 放顶层而非 data 子对象 | 0.2d | ⚠️ 兼容性写法（前端已 `d.subitemGroups \|\| d.data.subitemGroups`）| commit `f874259a` + `4209e256` 写 |
| 🟢 P2 | 数据 | E2 biz_agent_fee / biz_merchant_fee / biz_merchant_staff_invite 缺种子 | 0.3d | ❌ 未做 | `info schema` 0 行 |
| 🟢 P2 | 数据 | E3 biz_mp_auth / biz_settle_account / biz_settle_record 0 行（业务功能未实装或缺种子）| 0.5d | ❌ 未做 | `info schema` 0 行 |
| 🟢 P2 | 功能 | E4 推客二维码 admin 端管理（前端已调，admin 没界面看） | 0.5d | ✅ 已做 | 新增 `BizDistributorController.qrcode(distributorId)` + 前端「二维码」按钮/弹窗，复用 E10 缓存 |
| 🟡 P1 | 测试 | E5 ruoyi-system JUnit 5 单测基础设施 + C1 跨租户 guard 单测 | 0.5d | ✅ 已做 | `spring-boot-starter-test` + 3 个 `@MybatisTest`-等价单测（真 MySQL），反向验证 guard 删 → 2/3 FAIL |
| 🟡 P1 | 测试 | E6 subitem 创建 E2E smoke（create + read） | 0.3d | ✅ 已做（顺手修 DELETE 500 bug：removeGroup/removeSubitem 改幂等） | `.github/scripts/smoke-subitem.sh` 4/4 PASSED |
| 🟢 P2 | 工程 | E7 mvn spring-boot:run 本地启动 docs（README 加 1 段）| 0.1d | ✅ 已做 | README 2. 启动后端 段补 spring-boot:run + macOS 守护写法 |
| 🟢 P2 | 工程 | E8 mybatis xml lint（防止 e5fc6735 类「孤立 left join」回退）| 0.3d | ❌ 未做 | 49 个 mapper xml 无 lint |
| 🟢 P2 | UI | E9 admin 商品页「子品管理」入口 | 0.5d | ✅ 已做（doc 误判：详情底部子品搭配 section 早已实装） | 列表操作列加「子品」按钮 → 复用 handleUpdate 打开详情 → 详情底部子品搭配 section 自动展示 |
| 🟢 P2 | 工程 | E10 推客太阳码缓存（避免每次海报都调 wxacode）| 0.3d | ✅ 已做 | `ApiDistributorController.qrcode` 按 `qr_<memberId>_*.png` 缓存命中，命中返 `cached:true` 复用 URL；miss 才调 wxacode |

## 总工作量

约 3.5 天（5 个 0.1-0.3d 小任务 + 5 个 0.5d 中等任务）

## 建议推进顺序

1. **E1 subitemGroups 位置修正**（0.2d）—— 一行改动：`r.put("data", {product: p, subitemGroups: groups})` 或改前端
2. **E5 JUnit 单测覆盖**（0.5d）—— 加 spring-boot-starter-test + CommissionServiceImpl 单测
3. **E2 缴费种子数据**（0.3d）—— 类似 C1 subitem seed，为 agent/merchant/staff_invite 加演示数据
4. **E6 subitem 创建 E2E smoke**（0.3d）—— 类似 `.github/scripts/smoke-c1.sh` 加 subitem 端到端

## 不再建议做的事

- ~~删除 ApiPingController~~：生产用健康检查
- ~~删除 miniprogram7/tests/~~：真实 vitest 单测
- ~~集成 CI 端到端 smoke~~：macos-14 runner 缺 docker/mysql-client

## 与 8-05 doc 的关系

本清单是 8-05 清单 15 项 100% 闭环后的下个 10 项。
8-05 doc 状态见 `doc/下一轮迭代清单-2026-08-05.md` 末尾「全部闭环 🎉」段。

## 实施上下文

- 56 commit 全部已落 origin
- 后端 8080 进程在跑（PID 由 sandbox 决定）
- C1 smoke 3/3 PASSED 防跨租户回归
- 子品 7 商品 / 7 group / 14 subitem 已 seed
- 字节码级证据：`Map.put("totalAmount", overview.get("total_amount"))` 真编译
