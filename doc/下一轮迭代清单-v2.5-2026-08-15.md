# V2.5 迭代清单（2026-08-15 · 5 角色权限模型落地后）

> 来源：5 角色权限模型（PLATFORM/AGENT/OWNER/MANAGER/STAFF）`@RequireRole` 落地后，
> 商家端商品/订单/员工/财务管理的角色细粒度管控、代理商/平台 dashboard 仍待实装。
> 工作量单位：d（人/天）。

## 角色权限现状

| 角色 | userType | 已实装端点 | 缺细粒度管控的端点 |
|---|---|---|---|
| **PLATFORM** | `00` | `/api/.../platform/finance/summary`（demo） | 平台 dashboard（商家列表/订单流水/佣金提现/员工管理） |
| **AGENT** | `01` | 登录 + staffUser 标识 | 代理商 dashboard（名下商家/缴费/额度） |
| **OWNER** | owner | 登录 + 商家端 `/finance/summary`（demo） | 商品/订单/员工/提现/数据导出等 owner 专属功能 |
| **MANAGER** | manager | 登录 + `/finance/summary`（includeHigher） | 店内管理功能（订单处理/预约确认/部分员工管理） |
| **STAFF** | staff | 登录 + 核销/扫码 | 几乎全部数据接口都应 403 |

## 优先级矩阵

| 优先级 | 类别 | 项 | 工作量 | 状态 | 备注 |
|--------|------|-----|--------|------|------|
| 🟡 P1 | 安全 | V5-1 给所有商家端 ApiController 加 `@RequireRole` 注解 | 1.0d | ❌ 未做 | 现状：仅 `/finance/summary` 一个端点带注解；其他 ApiOrderController/ApiBillController/ApiBookingController 等都未做角色校验 |
| 🟡 P1 | 安全 | V5-2 平台 dashboard 完整化（商家列表 + 跨店订单流水 + 跨店员工管理） | 1.5d | ❌ 未做 | 现状：仅 `/platform/finance/summary` demo；需补 `/platform/merchant/list`、`/platform/order/list`、`/platform/staff/list` 等 |
| 🟡 P1 | 安全 | V5-3 代理商 dashboard 完整化（名下商家列表 + 商家营收汇总 + 缴费记录） | 1.0d | ❌ 未做 | 现状：仅登录 + staffUser 标识；缺 dashboard 端点和小程序 `/pages/agent/home/index` 业务 |
| 🟢 P2 | UI | V5-4 商家端工作台按角色显示不同卡片 | 0.5d | ❌ 未做 | OWNER 看「员工管理/财务/提现」；MANAGER 看「订单/预约/店内报表」；STAFF 仅「核销/扫码/今日数据」 |
| 🟢 P2 | UI | V5-5 平台端「跨店切换」组件（选商家 → 临时切换到该商家视角） | 0.5d | ❌ 未做 | 平台账号选中某个商家后，storeId/merchantId 临时覆盖，进入"模拟商家"模式 |
| 🟢 P2 | UI | V5-6 代理商端「切换名下商家」组件 | 0.5d | ❌ 未做 | 类似 V5-5，但范围限定为 agentId 名下商家 |
| 🟢 P2 | 功能 | V5-7 角色权限在 PC admin 端 sys_menu 的体现 | 0.5d | ❌ 未做 | 现状：admin 端用 sys_menu.perms 控制；商家端小程序没用 sys_menu 体系；需要给小程序端建立"角色-菜单"映射 |
| 🟢 P2 | 测试 | V5-8 smoke-c44: 5 角色对所有关键 ApiController 的权限矩阵验证 | 0.5d | ❌ 未做 | 现状：c43 只验了 2 个端点；需要遍历 ApiOrder/ApiBill/ApiBooking/ApiProduct/ApiMerchantStaff 全部端点 |
| 🟡 P1 | 功能 | V5-9 商品创建页按 role 限制（OWNER/MANAGER 可创建，STAFF 不可） | 0.3d | ❌ 未做 | 现状：商品创建仅在 ApiProductController 一处，但前端任何人都能进入；需要前端根据 `isManagerOrAbove` 显隐入口 |
| 🟡 P1 | 功能 | V5-10 员工管理页仅 OWNER 可见（MANAGER 只能看本店员工列表） | 0.3d | ❌ 未做 | 现状：员工管理是 PC admin 端的事；小程序端没实装员工管理 |
| 🟡 P1 | 安全 | V5-11 提现/佣金接口加 `@RequireRole(OWNER)` | 0.2d | ❌ 未做 | 现状：佣金/提现接口无 role 校验；MANAGER/STAFF 都能调 |

## 总工作量

约 7.0 天（3 个 P1 中等任务 + 3 个 P1 小任务 + 4 个 P2 中等任务 + 1 个 P2 测试任务）

## 建议推进顺序

1. **V5-1 给所有 ApiController 加 @RequireRole**（1.0d）—— 一次性把基线拉齐，避免任何商家端接口被 STAFF 误调
2. **V5-9 + V5-11 商家端核心入口按 role 限制**（0.5d）—— 商品创建、提现、佣金这 3 个最敏感
3. **V5-8 权限矩阵 smoke**（0.5d）—— 验证 P0 修复有效
4. **V5-2 + V5-3 平台/代理商 dashboard**（2.5d）—— 补齐 PLATFORM/AGENT 的业务可见性
5. **V5-4 + V5-5 + V5-6 UI 角色化**（1.5d）—— 前端按 staffUser.roles 显隐 tab/卡片
6. **V5-7 + V5-10**（1.0d）—— 角色-菜单映射 + 员工管理权

## 关联文档

- AGENTS.md 续篇 10（5 角色权限模型设计）
- `doc/不同身份权限与菜单评估.md`（PC 端视角的菜单过滤方案）
- `doc/下一轮迭代清单-2026-08-14.md`（上一轮 56 commit 的余项）
- `doc/PRD-抖音来客商品模型.md`（业务背景）

## 已实装（不在本轮清单）

- ✅ BizRole 枚举 + LoginMember.roles
- ✅ @RequireRole 注解 + RoleAuthInterceptor
- ✅ 商家端 /finance/summary（OWNER+MANAGER）
- ✅ 平台端 /platform/finance/summary（PLATFORM only，3 scope）
- ✅ 5 角色测试账号 + smoke-c43 25/25 PASS
- ✅ 小程序 staffUser 存 role + 5 视角首页路由
