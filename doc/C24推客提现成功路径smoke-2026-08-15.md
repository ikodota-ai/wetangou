# C24 /api/distributor/withdraw 成功路径 smoke · 2026-08-15

## 目标
member → join 推客 → fixture availableAmount=100 → 提现 30 → 验证状态机 + 余额冻结,12/12 PASS。

## 端点覆盖
| # | 端点 | 覆盖项 |
|---|---|---|
| 1 | POST /api/distributor/join | 准备推客档案 |
| 2 | DB fixture | available=100, frozen=0, withdraw=0 |
| 3 | POST /api/distributor/withdraw | amount=30 → status=0 (待审核) |
| 4 | DB 验证 | available 100→70 (申请时锁定) |
| 5 | DB 验证 | frozen=0 (待审核不冻结) |
| 6 | GET /api/distributor/withdraw/list | 含新记录 |
| 7 | GET /api/distributor/center | availableAmount=70, withdrawCount=1 |
| 8 | POST /api/distributor/withdraw | 超额 200 → 余额不足 (防御) |

## 状态机
| status | 含义 | 余额处理 |
|---|---|---|
| 0 (待审核) | 已申请 | available -= amount, frozen 不变 |
| 1 (审核通过) | 待打款 | c7 admin 端流程处理 |
| 2 (已驳回) | 退回 | available += amount (refund) |

## fixture
- member 走 mock appid login
- 兜底删除可能的残留 distributor/withdraw
- DB UPDATE 注入 availableAmount=100
- trap cleanup EXIT 保证 idempotent

## 历史覆盖对比
- c7 走过 admin 端 /biz/withdraw (审核)
- c21 F-) 走过 "余额不足" 防御
- C24 首次独立覆盖: 小程序端 withdraw 成功路径 + availableAmount 锁定 + 状态机验证

## 全套回归
- 36/36 smoke PASS (含 C24)
- 10/10 JUnit PASS
- 30/30 vitest PASS
