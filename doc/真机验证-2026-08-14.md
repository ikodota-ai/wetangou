# 真机验证清单（2026-08-14 5 轮累计 18 项改动）

> **本轮范围**：仅前端 (miniprogram7)。后端 jar `Aug 14 04:37` 不动。
> **本机 IP**：`172.20.10.2:8080`（config.js 已配 + 3 个 fallback 自动探测）。

## 0) 准备

```bash
ps aux | grep ruoyi-admin.jar | grep -v grep
# 没跑就启：
java -Dspring.profiles.active=prod -jar /Users/mac/dev/dytuangou/ruoyi-admin/target/ruoyi-admin.jar &

# 小程序：微信开发者工具 → 打开 miniprogram7 → 清缓存 → 重新编译
```

## 1) 一键 SQL 验证（30 秒）

```bash
mysql -uroot -p dytuangou < /Users/mac/dev/dytuangou/sql/verify_v3.sql
```

期望：10 行输出，每行 `cnt` 都是 `1`。如果有 `0`，跑 `sql/biz_merchant_v2.sql` 和 `sql/biz_product_model_v2.sql`。

## 2) 真机 14 步验证

| # | 步骤 | 期望 | 状态 |
|---|---|---|---|
| 1 | 冷启动首页 | 3.5s 内显示门店；distanceText 有数字或"计算中…" | ☐ |
| 2 | 首页 → 买单 → 走完支付 | 看到 "✓ 支付成功" 完成态页（金额大字） | ☐ |
| 3 | 完成态页 → 点"完成" | 跳回首页 tabBar | ☐ |
| 4 | 预约 Tab 进入（断网测试） | 5s 后显示"门店加载失败 ↻ 重新加载" | ☐ |
| 5 | 预约 → 选门店 → 选日期 → 选时段 | 时段列表正常 | ☐ |
| 6 | 我的预约 → 1 条数据 | 卡片占满整行（不是半宽） | ☐ |
| 7 | 我的预约 → 2+ 条 | 2 列展示 | ☐ |
| 8 | 商品详情 → 右上角"分享" | 弹窗 3 按钮有清晰图标（💬 ★ ⤓） | ☐ |
| 9 | 提交订单页 → 首次进入 | 手机号自动从 profile 拉到明文 | ☐ |
| 10 | 推客 → 点"立即成为推客" | 立即切换为已开通（不卡） | ☐ |
| 11 | 推客 → 点"分享" | 进入 poster 页（不弹"请先成为推客"） | ☐ |
| 12 | 商家端 → 登录 → 预约 | 点"确认到店" → 弹"已确认" | ☐ |
| 13 | 商家端 → 拒绝预约 | 填原因 → "已拒绝" | ☐ |
| 14 | 切 WiFi → 重新扫码 | 首页自动探测新 IP（不报"发送失败"） | ☐ |

## 3) 关键 Console 日志

```
[miniprogram] APPID => wx9e147c4e2151b123 | ... | BASE_URL => http://172.20.10.2:8080
[miniprogram] probeBaseUrl switched to http://172.20.10.2:8080
[pickNearestStore] source=... storeId=...
[home] pickNearestStore => {...}
[booking/detail] raw => {...}
[merchant booking] confirm OK => {...}  或  confirm FAIL => {...}
```

## 4) 失败回退

任一步失败 → 截图 + 复制 Console 关键日志。**14 步全部失败**通常是后端 jar 没启 / IP 不对 / v3 SQL 没跑。

## 5) 本会话实际改动文件（11 个前端 + 1 个 SQL + 1 个文档）

```
miniprogram7/utils/config.js                        [改] IP fallback + probeBaseUrl
miniprogram7/utils/request.js                       [改] _activeBaseUrl 动态切换
miniprogram7/pages/home/index.js                    [改] 3.5s 白板降级 + 距离兜底
miniprogram7/pages/booking/index.js                 [改] 5s 兜底 + retryStore()
miniprogram7/pages/booking/index.wxml               [改] 门店文案区分 + 重试按钮
miniprogram7/pages/booking/create/index.wxml        [改] 文案同步
miniprogram7/pages/booking/create/index.js          [改] null store 提示
miniprogram7/pages/booking/list/index.js            [改] _span: full/half
miniprogram7/pages/booking/list/index.wxml          [改] span-{{_span}} class
miniprogram7/pages/booking/list/index.wxss          [改] .span-half / .span-full
miniprogram7/pages/booking/detail/index.js          [改] 诊断 console.log
miniprogram7/pages/pay/index.wxml                   [改] 文案同步
miniprogram7/pages/goods/detail/index.wxml          [改] 分享按钮图标 emoji
miniprogram7/pages/goods/detail/index.wxss          [改] so-ic 居中
miniprogram7/pages/order/submit/index.js            [改] profile 双重剥层修复
miniprogram7/pages/order-pay/index.wxml             [改] paid-view 完成态
miniprogram7/pages/order-pay/index.wxss             [改] 完成态样式
miniprogram7/pages/order-pay/index.js               [改] onPaidDone + paid=true
miniprogram7/pages/promoter/index/index.js          [改] joined=true 强制 + poster 兜底
miniprogram7/pages/merchant/booking/index.js        [改] 详细错误日志 + showModal
sql/verify_v3.sql                                   [新] 一键验证 v3 SQL
doc/真机验证-2026-08-14.md                          [新] 本文件
```

## 6) 已知"未动"项（等你真机反馈后决策）

- v3 SQL 实际是否已执行（你跑 verify_v3.sql 就能知道）
- 商家端 confirm/reject 后端逻辑（如果日志报"非商家员工身份"，需检查 sys_user.userType 是否在登录时设了 'merchant'）
- 预约详情门店名（如果日志 [booking/detail] raw 里没 storeName，需跑 v2 SQL 重建 left join）
