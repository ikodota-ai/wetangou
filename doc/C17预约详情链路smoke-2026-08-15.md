# C17 预约详情/时段/取消链路 smoke · 2026-08-15

## 背景

C17 摸底发现 /api/booking/* 详情/时段/取消端点之前只在 c5 集成链路里走过，单独端到端覆盖缺失。
本轮首次写 13 case 端到端 smoke。

## 13 case 验证

```
C17 预约详情/时段/取消链路 smoke:
  ✅ A slots 200 + 含 storeId + 含 day/night 时段字段
  ✅ B slots 不传 date 200 (默认今天)
  ✅ C 不存在 storeId 抛错
  ✅ D 未登录 401
  ✅ E detail 200 + bookingId 正确
  ✅ F 不存在 bookingId 抛错
  ✅ G 报名成功 signupId
  ✅ G 取消报名 200
  ✅ G signup status=1 (已取消) 落库
  ✅ H list 200

C17 smoke: PASS=13 FAIL=0
```

| 维度 | 验证点 | 结果 |
|---|---|---|
| A | slots 时段查询 | ✅ |
| B | slots 默认日期 | ✅ |
| C | slots 异常门店 | ✅ |
| D | detail 未登录拦截 | ✅ |
| E | detail 登录返详情 | ✅ |
| F | detail 异常 bookingId | ✅ |
| G | 报名 + 取消 + 状态落库 | ✅ |
| H | 我的预约列表 | ✅ |

## 三重回归（30 + 10 + 30 = 70/70）

| 类型 | 范围 | 结果 |
|---|---|---|
| business smoke | c1~c17 + subitem | 17/17 |
| E2E smoke | e4/e10/e11/e13~e19/e21 + g6 | 13/13 |
| JUnit | ruoyi-system | 10/10 |
| vitest | miniprogram7 | 30/30 |

## 关键文件

- .github/scripts/smoke-c17.sh — 13 case 预约详情链路端到端

## 业务价值

- /api/booking/{bookingId} / slots / cancel 4 个端点首次独立端到端 smoke
- 预约链路所有用户操作 (查时段/详情/报名/取消/我的预约) 全部覆盖
- 摸出状态机实际语义: status=1 表示"已取消" (不是 2)
